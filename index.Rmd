<!-- badges go here -->
[![Say Thanks!](https://img.shields.io/badge/Say%20Thanks-!-1EAEDB.svg)](https://saythanks.io/to/JohnCoene)

```{r setup, include = FALSE}
knitr::opts_chunk$set(
  warning = FALSE,
  collapse = TRUE,
  comment = "#>"
)

library(htmltools)
```

```{r, echo=FALSE}
br()
br()

div(
  class = "row",
  div(
    class = "col-md-4",
    img(
      src = "logo.png",
      class = "img-responsive responsive-img"
    )
  ),
  div(
    class = "col-md-8",
    p(
      "Topic Modeling for Humans with gensim.",
      br(),
      "Large scale efficient topic modeling in R and Python."
    ),
    br(),
    p(
      tags$a(
        tags$i(class = "fa fa-rocket"),
        class = "btn btn-primary",
        href = "articles/installation.html",
        style = "margin-bottom: 5px;",
        "Installation"
      ),
      tags$a(
        tags$i(class = "fa fa-paint-brush"),
        class = "btn btn-default",
        href = "https://github.com/news-r/gensim",
        style = "margin-bottom: 5px;",
        "Github"
      )
    )
  )
)
```

## Example

```{r}
library(gensimr)

# latent similarity index
lsi <- model_lsi(common_corpus(), id2word = common_dictionary())
topics <- lsi$print_topics() # get topics
```

Objects returned by the package are not automatically converted to R data structures, please use `reticulate::py_to_r`, as shown below.

```{r}
reticulate::py_to_r(topics) # convert to R format
```
